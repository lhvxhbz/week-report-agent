# 自定义模板目录

此目录用于存放用户自定义的周报模板。

## 模板格式

每个模板为JSON文件，格式如下：

```json
{
  "name": "模板名称",
  "description": "模板描述",
  "template": "完整的prompt模板内容",
  "sections": ["章节1", "章节2", ...],
  "created_at": "创建时间"
}
```

## 如何添加模板

1. 通过Web界面上传周报文件并提取模板
2. 或手动创建JSON文件放在此目录
